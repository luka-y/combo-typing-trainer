


micro to-do:

priority 0:
    read the actual config, not default, created default if none
    make all config errors so they shown to the user with a prompt "press any key to exit"
    make better validation of all values, include InitLayout in config validation

    embed assets
    extract everything from just main to own files (refactor)
    test if all works on windows

priority 2:

priority 5:
    run profiler to see if getCombo or drawing (or anything else) needs optimization.
    make an ability to select font in config. get it from the system, instead of from the asset (?)
    make TrimDistanceBeforeCurrent and GenerateGibberishDistanceAfterCurrent calculated on startup based on screen width and face.

priority 10:


