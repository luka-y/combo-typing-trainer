


micro to-do:


priority 0:
    refactor to simplify by introducing/removing new types
        make so I don't have to have those index enums.
    extract everything I will have in the config to a separate file
    make config read functionality on startup
        that keymap per layout can be in config too. layout selection in config too
    check if all good with probabilities and if they make sense.
    clean up everything else, separate everything to own files.
    test if all works on windows

priority 2:

priority 10:


