


micro to-do:


priority 0:

priority 2:
    refactor:
        get rid of all of these layout switches and repeated maps
            have just the keys map, generate all other maps and slices to pick from from them
            have some kind of global struct for the current layout with pointers/values for all maps and slices
                separate symbols into lower and shifted too
        take a look at other things, there are a lot of things that feel like they can be reduced in complexity and get
            more readable at the same time.
    solve a bit broken probabilities by shift modifier combos.
    clean up main.go, move everything to its own files.
    make so consts-config has only things that are actually supposed to be changed by the user (move from main, move from there somewhere)
        OR maybe do some kind of config file and do it like a normal human being.
    test if it works on windows
priority 10:


